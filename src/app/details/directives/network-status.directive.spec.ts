import { NetworkStatusDirective } from './network-status.directive';

describe('NetworkStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new NetworkStatusDirective();
    expect(directive).toBeTruthy();
  });
});
